# 핵심 코드 위치 (Grep 키워드)

| 수정 대상 | 검색 키워드 |
|-----------|------------|
| 주문/진입 | `function openOrder` |
| 청산 | `function closePosition`, `closePositionUsdt`, `closePositionPct` |
| TP/SL 생성 | `makeTpSl`, `buildTpSlOrders` |
| 포지션 TP/SL 수정 | `applyPosTpSl`, `clearPosTpSl` |
| 미체결 발동 | `function checkPending` |
| 강제청산 | `function checkLiquidation` |
| 차트 초기화 | `function initChart` |
| 초기 봉 로드 | `function loadHistory` |
| 과거봉 추가 | `function loadOlder` |
| 지표 정의 | `INDICATOR_DEFS` |
| 지표 계산 | `calcSMA`, `calcEMA`, `calcHMA`, `calcBB`, `calcRSI`, `calcMACD`, `calcStoch`, `calcATR` |
| 지표 렌더 | `renderIndicator`, `renderAllIndicators`, `updateIndicatorsLast` |
| 오실레이터 차트 | `getOscChart` |
| 지표 UI | `renderIndicatorChips`, `openIndModal`, `addIndicator`, `removeIndicator` |
| 지표 설정 | `renderSettingsPanel`, `applyIndSettings`, `openIndSettings` |
| 알람 UI | `toggleAlarmPanel`, `saveAlarmSettings` |
| 알람 로직 | `checkHmaAlarm`, `fireAlarm`, `showAlarmToast` |
| 알람 오디오 | `_initAlarmAudio`, `_playAlarmBuffer` |
| 거래내역 | `renderHistory`, `logHist` |
| 포지션 렌더 | `function renderPositions` |
| 실시간 캔들 | `mergeCandle`, `liveTick`, `syncKlines` |
| 가격선 | `function drawPriceLines` |
| REST 폴링 | `async function pollLoop` |
| 그리기 엔진 | `function setupDrawing`, `redrawDrawings`, `onDrawDown` |
| 롱숏 도구 | `renderShape`, `posHandleAt`, `planQty` |
| 좌표 변환 | `tToX`, `pToY`, `xToT`, `yToP` |
| 그리기 저장 | `drawKey`, `loadDrawings`, `saveDrawings` |
| 청산량 단위 드롭다운 | `showCloseUnitDropdown`, `_setCloseUnit`, `_curMode` |
| 미체결 주문 편집 | `openPendingEdit`, `savePendingEdit` |
| 계좌 초기화 | `resetAccount` |
| TP/SL ROE% 공식 | `makeTpSl` |
