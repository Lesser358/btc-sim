# 핵심 코드 위치 (Grep 키워드)

| 수정 대상 | 검색 키워드 |
|-----------|------------|
| 주문/진입 | `function openOrder` |
| 청산 | `function closePosition`, `closePositionUsdt`, `closePositionPct` |
| TP/SL 생성 | `makeTpSl`, `buildTpSlOrders` |
| 포지션 TP/SL 수정 | `applyPosTpSl`, `clearPosTpSl` |
| 미체결 발동 | `function checkPending` |
| 강제청산 | `function checkLiquidation` |
| 차트 초기화 | `function initChart` |
| 초기 봉 로드 | `function loadHistory` |
| 과거봉 추가 | `function loadOlder` |
| 지표 정의 | `INDICATOR_DEFS` |
| 지표 계산 | `calcSMA`, `calcEMA`, `calcHMA`, `calcBB`, `calcRSI`, `calcMACD`, `calcStoch`, `calcATR` |
| 지표 렌더 | `renderIndicator`, `renderAllIndicators`, `updateIndicatorsLast` |
| 오실레이터 차트 | `getOscChart` |
| 지표 UI | `renderIndicatorChips`, `openIndModal`, `addIndicator`, `removeIndicator` |
| 지표 설정 | `renderSettingsPanel`, `applyIndSettings`, `openIndSettings` |
| 알람 UI | `toggleAlarmPanel`, `saveAlarmSettings` |
| 알람 로직 | `checkHmaAlarm`, `fireAlarm`, `showAlarmToast` |
| 알람 오디오 | `_initAlarmAudio`, `_playAlarmBuffer` |
| 거래내역 | `renderHistory`, `logHist` |
| 포지션 렌더 | `function renderPositions` |
| 실시간 캔들 | `mergeCandle`, `liveTick`, `syncKlines` |
| 가격선 | `function drawPriceLines` |
| REST 폴링 | `async function pollLoop` |
| 그리기 엔진 | `function setupDrawing`, `redrawDrawings`, `onDrawDown` |
| 롱숏 도구 | `renderShape`, `posHandleAt`, `planQty` |
| 좌표 변환 | `tToX`, `pToY`, `xToT`, `yToP` |
| 그리기 저장 | `drawKey`, `loadDrawings`, `saveDrawings` |
| 청산량 단위 드롭다운 | `showCloseUnitDropdown`, `_setCloseUnit`, `_curMode` |
| 미체결 주문 편집 | `openPendingEdit`, `savePendingEdit` |
| 계좌 초기화 | `resetAccount` |
| TP/SL ROE% 공식 | `makeTpSl` |

## 신호 스캐너 / AI

| 수정 대상 | 검색 키워드 |
|-----------|------------|
| 스타일 설정(스캘핑/단타/스윙/중기) | `SIGNAL_STYLE_CFG` |
| 신호 점수 계산(메인) | `function calcAiMarketSignalStyled` |
| 코인별 신호 로드/조회 | `loadAiCoinSignals`, `ensureAiSignalData` |
| 신호 스캐너 렌더 | `renderAiSignal`, `renderAiSignalRows`, `updateAiSignalDom` |
| 스타일 전환 | `switchSignalStyle`, `openSignalStyle` |
| 멀티 타임프레임 확인 | `_aiTfConfirm` |
| VWAP(스캘핑) | `_aiVwap` |
| RSI 다이버전스 | `_aiDivergence` |
| 거래량 흡수 | `_aiAbsorption` |
| 오더북 임밸런스/벽 | `_aiObImbalance` (depth fetch는 `loadAiCoinSignals` 내 `cfg.wOb>0`) |
| 신호 적중률 로깅 | `sigLogAndEval`, `sigAccuracy`, `_sigAccText`, `SIG_EVAL`, `SIG_STATS_KEY`(localStorage) |
| 신호 캔들 백테스트 | `runAiSignalBacktest`, `_sigBtStatsForSymbol`, `_sigBtSimTrade`, `_sigBtTradeCfg`, `_sigBtText`, `SIG_BT_KEY` |
| AI 분석(개인 거래 DNA) | `renderAiAnalysis`, `aiConsent` |
| 차트 위 AI 상승/하락 확률 | `renderAiProb`, `_scoreCandles`, `_aiProbCfg`, `_rawToUpProb` |
| 신호 보조 헬퍼 | `_aiClamp`, `_aiLastVal`, `_aiAvg`, `_aiKlines`, `_aiFetchJson` |
| 지표 설명 모달 | `showAiHelp`, `closeAiHelp` |

## 2026-06-25 검증 발견 버그 / 회귀 방지 위치

| 점검 대상 | 검색 키워드 |
|-----------|------------|
| 종목별 랭킹 대소문자 불일치 | `function logHist`, `computeRankStats`, `renderRankingBySymbol`, `symbols`, `rankSymbolSel` |
| Firestore 사용자 update 허용 필드 제한 | `firestore.rules`, `allow update`, `request.resource.data.keys().hasOnly`, `avatar` |
| 클라우드 상태 마이그레이션 공통화 | `loadState`, `applyCloudData`, `hmaProximity`, `hmaByIv`, `aiProb` |
| 신호 적중률 stale 기록 방지 | `loadAiCoinSignals`, `sigLogAndEval`, `_aiSignalReq`, `ensureAiSignalData`, `switchSignalStyle`, `toggleAiSignals` |
